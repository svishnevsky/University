@using GrSU.University.Clients.Resources
@using GrSU.University.Clients.Resources.Entities
@model GrSU.University.Clients.Web.Models.StudentGroups.StudentGroupModel

@{
    ViewBag.Title = StudentGroup.TitleEdit;
}

<h2>@StudentGroup.TitleEdit</h2>

@using (Html.BeginForm("Index", "StudentGroup"))
{

    @Html.HttpMethodOverride("PUT")
    @Html.AntiForgeryToken()

    @Html.HiddenFor(m => m.Id)

    @Html.EditorForModel()
}

<div>
    @Html.ActionLink(Navigation.GoToList, "Index", "StudentGroups")
</div>
